replicaCount: 1

image:
  repository: thinhops/frontend
  tag: "latest"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: ""
  annotations: {}
  hosts:
    - host: shopnow.local
      paths:
        - path: /
          pathType: Prefix
  tls: []