apiVersion: v1
kind: Secret
metadata:
  name: mysql-secret
  namespace: default
type: Opaque
data:
  # Base64 encoded password - replace with your actual password
  # To encode: echo -n 'your_real_password' | base64
  rootPassword: cGFzc3dvcmQ=  # 'password' encoded